范畴:
<ul>
<%= for category <- @categorys do %>
    <li><%= category.name %> </li>
<% end %>
</ul>
<br><br>
<%= for article <- @category.articles do %>
    标题: <%= article.title %>
    内容: <%= article.body %>
    <%= link "Show", to: category_article_path(@conn, :show, @category_id, article), class: "btn btn-default btn-xs" %>
    <br>
<% end %>

