<h2>Listing tags</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for tag <- @tags do %>
    <tr>
      <td><%= tag.name %></td>

      <td class="text-right">
        <%= link "Show", to: user_tag_path(@conn, :show, tag), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_tag_path(@conn, :edit, tag), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: user_tag_path(@conn, :delete, tag), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New tag", to: user_tag_path(@conn, :new) %>
